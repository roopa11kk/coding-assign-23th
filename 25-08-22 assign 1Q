#include<stdio.h>
#include<conio.h>
int main()
{
	char str[1000];
	int i,count=0;
	
	printf("Enter the string:");
	gets(str);
	
	for(i=0;str[i];i++)
	{
		if(str[i]==32)
		count++;
	}
	if(i>0)
	count++;
	
	printf("no of words in string = %d\n",count);
	
	return 0;
}
